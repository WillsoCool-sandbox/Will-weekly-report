
import { SavedReport } from './types';

// Exporting an empty array so the dashboard starts clean.
// You can upload your own PDF or Word reports to populate the data.
export const PRE_LOADED_REPORTS: SavedReport[] = [];
